package com.ruoyi.integration.taskdefinition;

/** Identifies the fixed execution semantics selected after a task revision is pinned. */
public enum TaskType
{
    REFERENCE_QUERY,
    OA_TO_U8,
    U8_TO_OA
}
