<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.integration.sync.schedule.SyncCursorMapper">
    <select id="selectForUpdate" resultType="java.time.LocalDateTime">
        SELECT cursor_time FROM int_sync_cursor WHERE task_code = #{taskCode} FOR UPDATE
    </select>

    <insert id="insert">
        INSERT INTO int_sync_cursor(task_code, cursor_time, create_time, update_time)
        VALUES(#{taskCode}, #{cursorTime}, NOW(), NOW())
        ON DUPLICATE KEY UPDATE task_code = VALUES(task_code)
    </insert>

    <update id="update">
        UPDATE int_sync_cursor
        SET cursor_time = #{cursorTime}, update_time = NOW()
        WHERE task_code = #{taskCode} AND cursor_time &lt; #{cursorTime}
    </update>
</mapper>
